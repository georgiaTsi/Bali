<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.amsterdam">

    <application
        android:allowBackup="true"
        android:icon="@drawable/amsterdam"
        android:label="@string/app_name"
        android:roundIcon="@drawable/amsterdam"
        android:supportsRtl="true"
        android:theme="@style/Theme.Amsterdam">
        <activity
            android:name=".DetailedWithTitleActivity"
            android:theme="@style/Theme.Amsterdam.NoActionBar"/>
        <activity
            android:name=".DetailedActivity"
            android:label="@string/title_activity_detailed"
            android:theme="@style/Theme.Amsterdam.NoActionBar" />
        <activity
            android:name=".NotesActivity"
            android:label="@string/title_activity_notes"
            android:theme="@style/Theme.Amsterdam.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>